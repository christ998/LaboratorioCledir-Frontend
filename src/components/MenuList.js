import {useState} from "react";
import {Box} from "@mui/material";

function menuList() {

    return (
        <div>
            <Box sx={{backgroundColor: '#ACDDF3', width: 300, height: 300}}>
                dfds
            </Box>
        </div>
    );
}

export default menuList